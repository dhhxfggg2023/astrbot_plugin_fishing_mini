feat: 鱼类等数据配置化（fish_defs）+ 可视化编辑器页面

数据配置化（第六阶段）
- 新增 fish_defs（text，默认 232 行）：id|名称|稀有度|基准价|分布|说明
  - 分布支持 钓点:权重（逗号分隔）、中文钓点名、* = 所有钓点、省略权重 = 1.0
  - 容错：空行/# 注释/全角标点/多余空格；坏行跳过并告警一次；分布为空的行跳过
  - 空值或全部写坏 → 自动回退内置鱼池；232 行数据放在 _fish_data.py，不撑大 main.py
  - 改价优先级：fish_value_overrides > fish_defs 行内价 × fish_value_mult > 内置
- 内容表排除「默认值自动同步」（fish_defs/location_defs/rod_defs/bait_defs/item_defs/
  aquarium_slots/backpack_upgrades）：站长编辑的内容不再被升级覆盖，官方新增内容由
  content_auto_merge 增量补；数值类仍照常自动同步
- 别名清理：删除死别名 鱼篓、删除 清理/junk（旧说明误导）、一键卖出 改指向 卖光光
- README 新增「在配置面板里改鱼与物品」一节（字段格式、分布写法、改价优先级）

可视化编辑器页面（第七阶段 A 组）
- 新增 pages/editor/index.html：单文件、零外部依赖、深浅主题自适应（跟随 isDark）
- 10 个标签页：鱼池/钓点/鱼饵/鱼竿/道具/杂物/变异/天气/数值/存档
- 行内编辑（Enter 提交 / Esc 还原 / Tab 跳格）、即时校验、搜索筛选排序、
  批量改价/改稀有度/改钓点/批量删除（内联确认）、脏标记与未保存拦截
- 存档页做成卡片时间线：可命名与就地重命名、恢复/下载/删除（内联二次确认）、自动备份表单
- 离线预览模式（检测不到桥接 SDK 时用自洽演示数据渲染），供无浏览器环境截图验收
- 新增 test_editor_ui.js：Node + 最小 DOM 桩的运行时冒烟测试（72 项断言），
  含「演示数据自洽性」护栏（分布引用的钓点必须存在、首屏不允许任何一行校验失败）
- docs/screenshots/ 归档三张界面截图（深色鱼池 / 存档页 / 浅色鱼池）

修复（第六阶段顺带发现）
- 权重换算漏乘稀有度权重 → 传说+神话出现率曾暴涨到 64.89%
- 导出时同一钓点重复鱼权重用覆盖而非累加 → 30 条鱼权重偏差
- 追加鱼种权重规则两条路径不一致（模块加载 ×0.7 vs 重建时旧保底 3.0）
- 配置写坏时回退路径不完整（原来只保留现状，现在按模块加载同一套步骤重建）

已知问题（留待下一轮）
- 等价性硬指标：鱼种/名称/稀有度/价值差异 0 条；仅 4 条鱼（reed_frog/dock_octopus/
  mangrove_mudskipper/trench_amphipod）在「清空配置回退内置」这条兜底路径上权重与旧版不同
  （名称复用 id 与追加鱼种 id 重叠所致），默认路径不受影响，测试已如实标注
- 杂物/变异/天气/彩蛋 4 个内容表尚未配置化（模式与 fish_defs 相同，待补）

测试：五套全绿（LOCAL/INT/LOAD/DOCKER/CI = 0），断言 503 → 525，
_conf_schema.json 与 DEFAULTS 85 项一致
