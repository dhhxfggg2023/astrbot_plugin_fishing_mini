# 待办 / 下一步开发

> 这个文件记录「已经和用户确认、但还没做」的需求，避免跨会话丢失。
> 完成一项就删掉一项；**已完成但容易踩坑的知识**放在文末「经验留档」，不要删。

## 当前没有待办

**v1.13.0 已完成**：① 按钮样式统一设置（策略 + 统一样式，默认行为不变）
② 效果键注册表（`_effects.EFFECTS` 一处生效）③ `extensions/*.py` 扩展点（含失败隔离、
示例扩展、README 章节、`test_effects_ext.py` 76 项回归）。

v1.10.0 的四项需求（玩家金币编辑 / 命令别名 / 自定义命令 / 配置面板瘦身）、
v1.11.0 的**养成道具重做**（三种角色：喂鱼 / 钓手手气 / 鱼缸装饰）、
v1.12.0 的**回复全配置化**（154 个回复场景：每条回复都能单独配按钮 + 改文案、
编辑器「💬 回复」卡片页 + 常驻预览、珊瑚造景可一次摆多个、`index.json` 不再空写）与
v1.12.1 的**紧急修复**（页面白名单写死 5 个场景 → 改命令别名被整页拒收；
改成「场景从插件接口实时取 + 保存只校验这次改过的行 + 精确到格子的报错与跳转」）
都已交付，详见 README 的 `### v1.12.1` / `### v1.12.0` / `### v1.11.0` / `### v1.10.0` 更新日志。
下一轮想做什么，先和用户确认再往这里写。

**v1.18.1**：站长提的「改用 sqlite 存数据」查证后是**已经在用 SQLite**（AstrBot KV 的
底层库），于是没有换存储，改为补上「旧作用域数据找回」（作者名改过导致老存档落在
旧 `scope_id` 里读不到）。结论与坑见文末 §14。

**v1.18.3**：站长的编辑器页面在窄 iframe 里「右边几列被切掉」，另外场景按钮的配置入口
跑到了「按钮总览」里 —— 前者按窄窗口拆卡片重做，后者改成点「＋」直接跳到场景卡片。
改页面 CSS / 表格前先看文末 §15。

**v1.18.4**：拆卡片之后站长指出真正的病根是**数值页的长列表挤在一行**（各钓点咬钩系数等）
把整行撑开 —— 现在长列表显示成可换行的小方块、编辑用多行输入框（一行一条）。
同样记在 §15。

**v1.18.5**：站长报「插曲给的手气不消失、还和锦鲤玉佩叠加」—— 一个 `luck_charges`
装了两种寿命的东西，拆成「一次性 `luck_charges`」+「持续型 `buff_quality`」，
取值取较高的那个（不叠加）。教训见文末 §16。

**v1.18.6**：站长报「点一个项再点另一个就跳回顶部」（整页重绘丢滚动位置，已修）与
「升级二次曲线没有推荐值」（数值页现在按当前值实时算出等级门槛 + 给推荐值）。
两条都记在 §15。

**v1.18.7**：个体品质加第 6 档「神话」，只能洗髓丹洗出来（很低的概率、可配）；
同一条鱼每天最多吃 3 颗洗髓丹（可配），吃满当天「厌恶」，次日恢复；洗髓丹 500 → 1200。
实现上的坑见文末 §17。

**v1.18.8**：① 个体品质改名（普通/优良/稀有/极品/传说/神话 → 凡品/良品/精品/珍品/绝品/神品），
和鱼种稀有度彻底分开，老存档按倍率自动换算；② 新增星陨湖 / 万水归墟 / 龙宫三个终局钓点
（+39 种水族，等级上限 67）；③ 升级不再重置站长改过的配置（见 §18）。

**v1.18.9**：站长报「连钓时锦鲤玉佩只消耗一次」—— 连钓路径原来整批只调一次
`_consume_luck`，现在改成**每竿各自结算与消耗**（空竿/杂物也算一竿，和体力/鱼饵一致），
结果里写明「本批 -N 竿　还剩 M 竿」。教训见 §19。

**v1.18.10**：站长报「连钓的鱼居然不会跑……连钓最多有鱼没咬饵」—— 连钓与单竿是两套逻辑：
连钓直接拿鱼种**标称逃脱率**判一次（单竿里那 30% 是「没拉到」的概率），于是同一批鱼
单竿会跑、连钓不跑；连钓空竿又只写「💨 空竿」（单竿写「咬了一口又吐掉了——蚯蚓 白搭了」），
而且**一个饵都不扣**。现在：① 连钓按 `鱼种逃脱率 × multi_escape_mult`（默认 2.5，
传说 30% → 75%、封顶 95%）判一次，跑掉的那条把用的概率写出来；② 空竿的三种说法
（空钩 / 深水 / 被鱼咬掉）抽成 `_calc._miss_flavor`，**单竿与连钓共用**；
③ `_calc._bait_consumed` 统一扣饵规则：文案说「白搭了」就真扣，只有「没咬钩 / 鱼不开口」不扣。
教训见 §20。

**v1.18.11**：站长报「事件的运气没有和锦鲤玉佩叠加，他们是不同种效果，应该可以叠加，
只不过一竿过后事件的效果会消失」—— v1.18.5 修「插曲手气赖着不走」时顺手把叠加
也一起禁掉了（`_effective_luck` 用 `max()`），属于**过度修正**：两个字段各管各的寿命之后，
叠加本来就不会残留。现在改回相加。教训见 §16（末尾那段）。

**v1.18.12**：站长报「新的钓点怎么没把生物那些加上，游戏里面不显示」——
`fish_defs` 是「一整段多行文本 + 非空就整体接管鱼池」，而通用内容合并只认 list，
于是那 39 种新鱼一条都没进他的配置，三个新钓点成了**空池**（钓点本身靠 location_defs
的 list 合并补上了）。现在 `_merge_fish_defs()` 会按 id 追加缺的鱼，
`CONTENT_ROW_FIXES` 也认字符串形态的内容表了。教训见 §21 与「新增钓点/新增鱼」那一节。

**v1.18.13**：站长要的三件事 ——① 事件全连起来、能「连续剧」（新增 4 条连载 ×3 话 + 4 条 `require` 门控的后续插曲，
   选择记成旗标，下一话接上一话的结局；选项位置每次随机）；
② `/钓鱼 商店` 拆成鱼竿/道具/鱼饵三家，老写法只回指路（还带着「你原本想买的那件该去哪家」）；
③ 背包扩容 3→7 档、鱼缸扩建 3→6 档（后者新增 `名字|价格|加几位`）。
教训见 §22（给「内容表 + 存档状态」加新维度时的自查清单）。

**v1.18.14**：站长问「手气的具体效果」时顺手查出来的两处不一致 ——
① 拉线的鱼（传说/神话）只吃「鱼饵+天气+评价加成」，**鱼竿与玉佩/插曲手气没传进去**，
   而收尾照样扣玉佩额度；② 兜底鱼竿 `RODS` 与 `rod_defs` 数值不一致（清空配置就换一套）。
两条都是「同一个东西存在两份/两条路径」的老毛病，教训见 §23。

**v1.18.15**：站长问「数值平衡吗、后期要不要加竿加饵」，拿他真实配置跑了每竿期望收益，
按结论补了**两档后期竿**（新增两列：拉线窗口加成 / 逃脱率系数）+ **两档专精饵**
（常见/少见权重压低、传说/神话抬高，第一次让「用哪款饵」成为取舍），
并把挂机单次封顶 5000 → 10 万。数值思路见 §24。

**v1.18.16**：这一轮是站长一次提的五件事一起做 ——
① 「重新写所有文本，并增加事件的文本量」：49 个随机插曲（29 个带前置条件、5 组双旗标联动）、
   6 条连载 × 3 话 = 18 话、26 张纸条、32 条静态文案全部重写；
② 「`/钓鱼 查` 最好所有能查的都能查，但游戏文本要服务游戏」：扩成九类可查 + 精确同名优先
   + 卡片限长（边界见 §25）；
③ 「锦鲤玉佩数值是不是太高」：+30%/500 金 → +20%/6000 金，道具整套重设成 13 件并补上
   解锁等级（迁移规矩见 §27）；
④ 「扩容没生效 / 自动存档好像不生效」：三个「悄悄坏掉」的坑（见 §26）；
⑤ 「编辑器要能编辑插件的所有东西，所有」：113 项配置一个不漏 + 新「全部配置键」视图
   + 全页文字换行 + 场景按钮改成从已有按钮里挑 + `backup_dir` 放开
   （还顺手修掉白名单里 `backup_upgrades` 的拼写错误，见 §28）。

### 新增钓点/新增鱼时要注意（v1.18.8 踩到的坑，v1.18.12 补了第 6 条）

* 新钓点要同时改 **5 处**：`main.LOCATIONS`、`main.LOCATION_TIER_ORDER`、
  `main.TIER_BASE_VALUE` + `DEFAULTS["tier_base_values"]`、`DEFAULTS["location_hook_factors"]`、
  以及 `_fish_data.LOCATION_ROSTERS`（鱼名单）；
* `_fish_data.FISH_DEFS_DEFAULT` 是「当前鱼池的文本形态」，**新鱼必须按格式追加进去**
  （否则 `content_auto_merge` 补不到老配置）。格式：`id|名称|稀有度|基准价|分布|说明`，
  其中**基准价 = 档位基准 × 稀有度系数 × `_stable_jitter(id)`**，
  **分布里的权重 = `_stable_jitter(id, 0.85, 1.15)`**（4 位小数）——
  ⚠️ 别写成追加鱼种那种固定的 `0.7`：`_apply_fish_defs` 会拿它乘稀有度权重，
  和内置路径（`ROSTER_RARITY_WEIGHT × jitter`）对不上，`test_local.py` 的
  「两条构建路径逐项等价」会直接红（这次就是这么发现的）；
* 改完跑 `test_local.py`，它会断言「19 个钓点的常规鱼种都能抽到」与两条路径逐项等价。
* ⚠️ **第 6 处：老配置里的 `fish_defs` 快照**（v1.18.12 才补上，站长就是被这个坑到的）——
  光把新鱼写进 `FISH_DEFS_DEFAULT` 不够，因为站长配置里那份 `fish_defs` 是旧快照，
  而它**非空就整体接管鱼池**：新钓点会变成「进得去、钓不到、图鉴空白」。
  现在 `_merge_content_defaults` 里的 `_merge_fish_defs()` 会自动按 id 追加缺的行
  （只对「至少一半的行是官方鱼 id」的旧快照下手，手工鱼池不动）。
  **加新鱼后请顺手在测试里确认**：`test_local.py` 的 `[10u]` 那段就是拿
  「官方池 − 新鱼」造一份旧快照，断言补完能到 271 种、三个新钓点非空。
* 如果某一行的**内容**（价格/分布/说明）官方改了，光靠追加是推不到老配置的
  —— 要么登记 `_game_data.CONTENT_ROW_FIXES`（旧整行 → 新整行，逐字相等才替换），
  要么接受老配置保持旧值。大肥鱼的分布（v1.18.12 补 3 个新钓点）就是走的这条路。


### 站长侧待办（不是代码问题，需要他自己点一下）

- 他配置里的 `command_aliases` **仍然缺「排名」**（那次编辑被 v1.12.0 的保存闸门吞了）：
  升级到 v1.12.1 后**刷新编辑器页面**，在「⌨️ 命令」页给 `排行` 那行补上 `,排名` 再保存即可。
- 他配置里的 `button_defs` 有 4 行用了 `cast.miss_none` / `cast.miss_bait` /
  `cast.miss_deep` / `cast.multi_summary` —— 这 4 行本来就是合法的，升级后不再判红。
- 遗留目录 `x5_eyhfj4p4/` 需要他在插件目录下手动 `Remove-Item -Recurse -Force`。

### 回复场景体系（v1.12.0）的维护约定 —— 改代码前必读

1. **场景清单是唯一真相**：`_calc.py` 的 `SCENE_GROUPS` + `REPLY_SCENES`（场景 id、
   分组、说明、继承的父场景）。新增一条给玩家看的回复 = 在这里加一行 + 在
   `_texts.py` 的 `TEXTS` 里加同名键（两处必须一一对应，`test_local.py` 第 18 组断言会红）。
2. **回复出口必须带场景**：所有 `event.plain_result(...)` 都要包在
   `async for _r in self._say_msg(event, "场景", event.plain_result(...)): yield _r`
   里（或 `self._say(event, 文本, "场景", values=...)` / `await self._push(event, "场景", 文本)`）。
   裸写 `yield event.plain_result(...)` 会被第 18 组的 AST 扫描抓到。
3. **默认行为不许变**：新场景默认**没有按钮**（`REPLY_SCENES` 的继承字段留空）；
   老的 5 个场景（cast/pull/bag/location/story）仍然是内置默认按钮的拥有者，
   从它们拆出来的 7 条回复靠「继承」沿用那一组。文案的默认值 = 代码原本的字符串，
   `text_overrides` 留空时一个字都不变（默认文案由「由代码拼装」决定是
   `{原文}` 还是可整段改写，见 `_texts.TEXTS`）。
4. **改文案/按钮的运行时链路**：`_say` / `_say_msg` / `_push` → `_views._scene_text`
   （文案覆盖，含父场景继承）→ `_texts.render_scene`；按钮走 `_views._scene_items`
   （自己配的 > 继承父场景 > 内置）→ `_layout_rows`（按 `button_layout` 排版）。
5. **占位符要真的传值**：`_texts.EXTRA_PLACEHOLDERS` 里登记了某个场景的额外占位符，
   就要求对应调用点通过 `values={...}` 把值传进来；只登记不传值 = 站长写了也渲染不出来
   （渲染失败会安全回退原文，但这是静默失效，属于坑）。
6. **编辑器页面**：`scenes` 端点（`_editor_bridge.py`）给页面喂场景卡片数据，
   保存走 `config` 端点的 `save_replies` 动作（只提交改动的那几项）。
   页面断言在 `test_editor_ui.js` 的 `[13]~[15]` 组，改页面后必须 `node test_editor_ui.js`。

## 建议的下一步（**未与用户确认**，仅备选）

- 鱼缸装饰目前只有「加成挂机产出」一种作用；若用户想要「装饰也能提升鱼的成长速度」
  或「装饰有互斥/组合效果」，在 `_decoration_bonus()` 里扩展，注意
  `_prune_decorations()` 要在**每次读取前**先剪过期项。
- 钓手手气道具只有 `buff_quality` 一种（提升个体品质概率）。若要做「提升稀有鱼出现率」
  的手气道具，需要给 `buff_casts_left` 配一个「作用类型」字段，否则两种 buff 会互相顶掉。
- 玩家页目前只能改金币；如果用户想要「重置某个玩家 / 补发鱼饵」这类操作，
  照 `_editor_set_player_gold` 的写法加动作即可（**只加白名单字段**，仍然二次确认 + 改前自动存档）。
- 别名支持「少打空格」已经可用；若用户要求别名也能改**内置写法的含义**
  （例如让 `/钓鱼 包` 变成别的子命令），需要改分派链而不是别名表 —— 目前是刻意不做的。
- 自定义命令若用户想要更多动作（例如 `发送:` 附带图片、`执行:` 带权限判断），
  在 `_calc.CUSTOM_COMMAND_ACTIONS` 里加类型 + 在 `fishing()` 的自定义分支里加执行分支。

---

## 经验留档（踩过的坑，勿再走弯路）

### 1. 数据隔离（最重要，曾造成不可恢复的损失）

`_refresh_config()` 会按配置项 `backup_dir` **重建** `self.backup_store`，
所以测试里只赋值 `backup_store` 是拦不住的 —— 必须在 `make_plugin()` 里
把 `backup_dir` 钉进系统临时沙箱（现在就是这么做的 + 有断言），
并且在任何删除/清理类用例前断言「存档根目录确实在沙箱里」。
历史上因为这条，测试把真插件目录里 55 份手动快照删掉了（不可恢复）。
`test_local.py` 的 `[17]` 还加了一条「真插件 `backups/` 前后零变化」的自检。

### 2. 编辑器页面通道（已打通，勿再走弯路）

- 插件页面 SDK 的 endpoint 会被拼成 `/api/v1/plugins/extensions/<插件名>/<endpoint>`；
  该路径由 AstrBot 的 `_call_plugin_extension` 在 `star_context.registered_web_apis` 里匹配，
  匹配不到就是「未找到该路由」。
- 因此**插件必须自己注册 Web API**：
  `context.register_web_api("/<插件名>/config", handler, ["GET","POST"], desc)`
  —— route 必须带插件名前缀，页面只传相对路径 `config`。
- 现有 4 个路由：`config`（GET 读 / POST 写）、`snapshot`（存档动作）、
  `players`（玩家列表 / 存档内玩家 / 改金币）。**加新路由就照这个模式加一条 route spec。**
- ❌ 不要再用「上传文件 + 轮询」通道：真实环境返回 `403 Insufficient API key scope`。
- ❌ 不要再猜 `/api/plugins/<id>/config` 这类路径：那是 AstrBot 自己的接口，不在插件页面权限内。
- ⚠️ 页面里的 `sendCommand()` 会把 `snapshot_` 开头的动作**自动改投到 `snapshot` 路由**并去掉前缀 ——
  名字里带 `snapshot_` 但实际不属于存档的动作（例如 `snapshot_gold`）必须直接 POST 到自己的路由，
  否则会被改写成 `{action: "gold"}` 打到存档接口上（v1.10.0 真的踩到过，靠 `test_editor_ui.js` 抓到）。

### 3. 改 `pages/editor/index.html` 的硬规矩

- **只做最小改动**：先 `read` 再 `edit`，**永远不要用正则整段替换函数**（已经两次把页面写坏）。
- **每改一处立刻跑 `node test_editor_ui.js`**（离线 + 假 SDK 两种通道都会跑）。
- **页面里任何「插件侧才有的清单」都不许写死**（v1.12.1 的教训，代价是站长存不了配置）：
  场景键有 154 个、还会随版本增加，写死的白名单会把合法数据判成红线。
  需要清单就从插件接口拿（`scenes` 端点给了 `scene_total` 和每个场景），
  拿不到时**返回 null 表示不校验**，并且**永远不要拿校验结果去拦整页保存**。
- **保存闸门只拦「这次改过的行」**：`doSave()` → `diffTab(id).dirty` → `problemsInTab(id, true)`；
  没动过的行里的问题走 `stale` 分支只提醒（`tabHealth()` 给标签上的 ⛔/⚠ 供数）。
  报错必须给出「表 · 行号 · 行标识 · 字段 · 原因」，并用 `gotoProblem()` 跳过去 ——
  只说「有内容没填对」等于让站长自己一格格找，历史上就是这么丢掉他的别名编辑的。
- 加标签页时要同步：`TABS`、`TABLE_DEFS`、`state.data` 初始化、
  `test_editor_ui.js` 里的标签数 / 载荷字段数 / 序列化键清单断言。
- `invisible` 是**显示开关**：`_config_schema_to_default_config()` 不看它，
  所有项照样进默认配置；AstrBot 前端的 `AstrBotConfigV4` 用 `l.invisible` 决定要不要渲染那一行。
  所以「隐藏」不会让配置值丢失，也不会让编辑器页面改不了（页面白名单来自 `DEFAULTS`）。

### 4. 配置默认值同步

- `_defaults_fingerprint()` 只对**参与同步**的键做哈希；新增配置项若属于
  「站长自己写的内容」（内容表、命令别名、自定义命令）或纯说明项，
  必须加进 `DEFAULTS_SYNC_EXCLUDE_KEYS`，否则升级时会把站长的编辑覆盖掉，
  也会让指纹变化触发一次全量同步。

### 5. 道具效果键（v1.11.0 的老规矩；v1.13.0 起简化成一处，见 §7）

效果键的白名单在**三个地方**，改的时候一起改，否则会出现「配置里写了但没生效」：

1. `_calc.py` 的 `_parse_effects()` —— `allowed` 元组（真正的解析口，写错的键**静默忽略**）；
2. `pages/editor/index.html` 的 `ITEM_EFFECT_KEYS` —— 页面提示 + 行校验（写错会标红）；
3. `test_editor_ui.js` 会**直接读 `_calc.py` 的源码**断言两边键集合一致，所以漏改会立刻报错。

三种角色的语义（别混）：

- 喂鱼 `meat/spirit/sheen/value_up` → **一次性永久**，落在某条鱼的实例上；
  `feed_bonus` 只抬这条鱼的**投喂次数上限**（`feed_max_uses` + 累计 `feed_bonus`，夹在 0~20）。
- 钓手手气 `buff_quality` → 落在**玩家**身上，`luck_charges` + `buff_casts_left`；
  `_engine.py` 每竿扣一次 `buff_casts_left`，**只有扣到 0 才清 `luck_charges`**
  （彩蛋捡到的手气没有竿数，仍然一竿即清）。**别在喂鱼分支里改玩家状态。**
- 鱼缸装饰 `decorate` → 落在 `player["decorations"]` 列表，每条有自己的 `expire_ts`；
  加成在领取挂机收益时用 `_decoration_bonus()` 现算（时间按真实时间，下线也走）。

### 7. 效果键现在只有一处（v1.13.0）

加一个内置效果键，只需要在 `_effects.py` 的 `BUILTIN_EFFECTS` 里加一行 `EffectSpec`：

* 解析白名单：`load_extensions()` / `_apply_extensions()` 会调 `sync_to_calc()`，
  把键名同步进 `_calc.EFFECT_ALLOWED`（解析实现仍在 `_calc._parse_effects`，
  数值写法与历史版本逐字一致）
* 页面清单：`_editor_bridge` 把 `effect_table()` 塞进 `config` 与 `scenes` 两个接口，
  页面 `applyEffectKeys()` 覆盖内置兜底清单 —— **别再往页面里写死清单**
* 编辑器校验：页面用插件发来的键名判断「插件不认识的效果键」，拿不到就**不校验**
  （宁可漏报，也不能把本来对的行标红 —— v1.12.1 的教训）

配套的坑：

* `test_editor_ui.js` 会去读插件源码比对白名单。白名单从 `_parse_effects` 函数体搬到
  模块级 `EFFECT_ALLOWED` + `_effects.BUILTIN_EFFECTS` 之后，**测试的正则要跟着改**
  （现在同时校验「注册表顺序 == 白名单顺序」）
* `TABLE_DEFS` 是页面加载时建的静态对象，`title` 已经把效果说明快照下来了；
  `applyEffectKeys()` 里要顺手把它同步回来，否则表头 tooltip 还是旧的
* `_effects` / `_calc` 是 `_load_sibling` 按**别名**注册的（`astrbot_fishing_effects`）。
  兄弟模块里要拿对方，必须先查 `sys.modules`，裸 `import _calc` 会拿到另一个实例
  （白名单里没有扩展键，扩展键就解析不出来了 —— 单测里踩过）
* 扩展处理函数的签名固定为 `(*, plugin, player, item_id, key, value)`；
  只有扩展键的道具由插件代劳「消耗一件 + 保存 + 提示」，和内置效果混在一件道具上时
  提示仍由内置分支负责（扩展的文字不单独显示）

### 8. 按钮样式：0 = 灰、1 = 蓝（v1.13.1 更正，别再猜）

**官网取值就是 0 和 1，别自己发明数字。**
《消息按钮》文档写得很死：`render_data.style | int | 是 | 按钮样式：0 灰色线框，1 蓝色线框`
（bot.q.qq.com/wiki/develop/api-v2/server-inter/message/trans/msg-btn.html）。
`botpy` 的 `RenderData.style` 只是个裸 `int`（没有枚举），所以文档就是唯一定义。

* v1.13.1 之前 `_calc.BUTTON_STYLE_ALIASES` 写的是 `default=1 / primary=4` —— 纯猜的。
  后果：标着「默认（灰）」的按钮在 QQ 里是**蓝色线框**，而 `primary=4` 根本不在文档里。
  现在 `default=0`、`primary=1`，`BUTTON_STYLE_DEFAULT = 0`，`_interactions._btn` 兜底也是 0。
* **页面与插件必须用同一套数字**：`pages/editor/index.html` 的 `normalizeButtonStyle()`
  把 `0→default`、`1→primary`，其余数字（2~255）**原样透传**。
  页面认不出的数字绝不能改写成 default —— 那是「保存一次就把玩家的按钮颜色全改了」。
* 改这张表 = 改站长线上按钮的颜色。要改先想清楚：老配置里 `|default` 的按钮会换色。
* 数值样式（`|7` 这种）任何环节都不要翻译、不要四舍五入，官网只保证 0/1 的含义，
  其余数字是「原样透传」，插件不替 QQ 做解释。

### 9. 订单为什么按钓点抽（v1.14.0）

`_roll_orders(level, location_id=None)`：

* 给了 `location_id` -> 候选池 = `_location_pool(location_id)`（**当前钓点**）；
  首选 = 等级品质档内的鱼，补充 = **同一个图**其它品质的鱼。本图这一档不够时用
  同图的鱼补满（`order_count` 不缩水），**绝不跨图** —— 「站在哪儿只能被点哪儿的货」
  是硬约束，不然又会出现「要一条你根本钓不到的鱼」（v1.14.0 之前就是这样：
  池子是全部 232 种，会点到还没解锁的钓点的鱼）。
* `location_id=None` -> 老行为：全鱼池里按品质档抽，补充池为空。
* ⚠️ 抽签顺序不能反：`_order_pools()` 返回 `(首选, 补充)`，**先抽首选、不够再补**。
  直接 `random.sample(首选 + 补充)` 会把等级品质档稀释掉
  （Lv20 站在新手村就会一直点常见鱼）。

换钓点换单（`_ensure_orders`）：

* `expired = (没订单) or 到点了` -> 换一批，并把 `order_move_rerolls` **清零**；
  没到点但 `order_location != current_location` -> 也算要换（`moved`），
  但要受 `order_move_rerolls` 限制（默认 1）。
* ⚠️ 顺序陷阱：一开始写成「只要 `moved` 就 +1」，结果**到点刷新和换图撞在一起**时
  计数被 +1 而不是清零，玩家在新周期里白白少一次换单机会。现在 `expired` 优先判断。
* 换单**不重置** `order_next_ts`（否则来回换图 = 无限刷单）；
  每个周期最多换 `order_move_rerolls` 批，这就是防刷单的那道闸。
* 移动回复只在「本来就有订单 + 真的换了」时才多一行，别在没看过订单的玩家那儿刷屏。

`/钓鱼 去` 与 `解锁并前往` **两处**都要调 `_ensure_orders`（`_cmd_locations` 里
两个 `player["current_location"] = ...` 各一处），漏一个就会出现「换图了订单没跟」。

测试口径：`test_local.py` 的「订单按当前钓点刷新」段 —— 抽 200 批断言零越界、
换图换单 + 计数、上限拦住第二次换图、到点刷新清零、开关关掉完全回老行为、
隐藏生物开关、`order_move_rerolls=0`。**别只测「抽出来的鱼在图里」**，
「不换单」那条路径也要钉住，否则防刷单被改坏了没人知道。

### 10. 水族馆相处机制：方向比强弱更重要（v1.15.0）

> ⚠️ **v1.18.17 已整套删除** —— 站长原话「将狠角色这一设定删去，没意思，容易造成巨大损失」。
> 下面这段留着当「当时怎么想的」的存档；要删类似机制时照 §30 的清单走。

站长报的「藤壶怎么把章鱼吃了」不是概率问题，是**规则方向**写错了：
旧版是「狠角色 + 对手（另一个狠角色或紧挨着的邻居），谁 `_fish_power` 低谁没」。
于是养肥的藤壶（`_fish_power` 高）能把章鱼冤死 —— 文案还写成「藤壶把章鱼逼到了角落」，
等于说藤壶是凶手。**温和鱼根本不该有下嘴的能力。**

现在的规则（`_resolve_tank_conflicts`）：

* 只有 `_is_hostile` 的鱼会主动攻击；
* 对手是另一个狠角色 -> 壮的吃弱的（duel，文案「打了一架，X 没了」）；
* 对手是温和鱼 -> 狠角色**必须 `_fish_power >=` 对方**才吃得下，
  否则 `continue` 换下一个狠角色试（小章鱼挨着养肥的大鱼：两条都活着）；
* 温和鱼之间永远无事发生。

坑与约定：

* 一轮最多淘汰一条，淘汰后重新扫描（外层 `for _ in range(len(aquarium))` +
  内层找到能下嘴的就 `break`，靠 `acted` 判断「这轮没人动手」就收工）——
  不要写成「每轮固定换掉一条」，否则同一个狠角色会被反复配对。
* **不要用 `diff`（拉线难度）当捕食者信号**：实测章鱼 diff=0.0、月华水母 diff=0.62，
  两者完全分不开。分类只有 `HOSTILE_KEYWORDS` 一张表。
* `_parse_word_list("")` 返回**内置默认**（不是空元组）：所以「清空关键词 = 恢复默认」，
  页面文案也照这个说。想让缸里彻底没有狠角色，只能写一个匹配不到任何鱼名的词。
* 页面侧要改这张表，靠 `NUMBER_KEYS` 第 5 位 `true` 标成文本行：
  `cellHtml` / `onCellInput` / `validateRow` / 对齐 class 都判了 `row.text`，
  **漏一处就会出现「需要是数字」或者把逗号串 `Number()` 成 NaN**。
  加断言见 `test_editor_ui.js`「文本行…」四条。
* ⚠️ 探针里 `import _editor_bridge as eb` 拿到的是**另一份模块实例**
  （`DEFAULTS` 没注入 → `number_whitelist()` 返回 0 条，看着像「白名单没这个键」）。
  要验证页面通道，必须用插件按别名加载的那份：`sys.modules["astrbot_fishing_editor_bridge"]`。
  同样的坑 `_calc` / `_effects` 也有（见 §7）。

### 11. 页面校验用的是插件下发的清单 —— 下发什么就得包含什么（v1.15.1）

站长报「UI 里洗髓丹那行报错：不认识的效果键」。那行写的是 `quality_up=0.30`，
而 `_calc._parse_effects` 一直照收（别名 -> `buff_quality`）：**插件认，页面不认**。

根因：v1.13.0 把页面的写死清单换成 `_effects.effect_table()` 下发，
而 `as_dict()` 只给了**规范键名**，没给 `EFFECT_ALIASES` 里的旧写法。
页面于是拿着一份「少了别名」的清单去校验，把一行完全能用的配置标红。

规矩：

* 注册表里凡是**解析器会接受**的键，都必须出现在 `effect_table()` 的 `as_dict()` 里
  （现在每行带 `aliases`），否则页面就会误报。加别名的同时要加断言：
  `test_effects_ext.py`「每行都带 aliases」+ `test_editor_ui.js`「旧写法也进白名单」。
* 页面**只接受、不改写**：`ITEM_EFFECT_ALIASES` 只用于认键和写 tooltip，
  绝不把站长写的 `quality_up` 偷偷换成 `buff_quality`。
* 给页面下发清单这类「单一来源」改造，**改完要拿站长的真实配置跑一遍**：
  这次就是他的 `item_defs` 里有旧写法才暴露出来的（实测那行和「锦鲤玉佩」
  解析结果一模一样 —— 洗髓丹是 v1.11.0 之前的遗留行）。

见闻（`hostiles_seen`）的约定：

* 只在**当着玩家的面**发生捕食时记录，记的是**凶手**（`winner`）的鱼种 id；
  `_resolve_tank_conflicts` 因此返回三元组 `(文案, 有没有变, 被看破的鱼种)`，
  调用点（`水族馆 放`）负责并进玩家数据 —— 别在结算函数里直接改玩家字典。
* 展示只在 `/钓鱼 查 <鱼名>`（他要求的入口）。**不要**顺手加进图鉴/背包，
  那些是共享 helper，一行一行改容易漏。
* 老存档没有这个字段 -> `_calc` 读档时补空表，老玩家开局同样是「不知道」。

### 12. 「展示类收益」必须按时间算，不能按当前状态算（v1.16.0）

水族馆的展出加成（`aquarium_bonus`，取出/卖出时 ×1.2）踩了两次同一个坑：

1. v1.x 早期：每次取出都加一次 -> 「取出→放入→取出」无限刷钱，用 `pond_claimed` 锁成终生一次；
2. v1.16.0 之前：**放进去一秒再取出来照样算一次**（站长报的「那 1.2 的倍数不能立刻
   放入取出就有」）——「只能领一次」根本挡不住这个。

现在的规则：鱼进缸时记 `tank_since`，出缸时把这段时长**累加**进 `tank_seconds`，
取出/卖出时要求 `tank_seconds >= aquarium_bonus_min_hours × 3600`；
不够就返回 `too_new`、**不打 `pond_claimed`**（放回去接着攒，累计制）。

坑与约定：

* ⚠️ `_repair_instance` 是**白名单式重建**：`tank_since` / `tank_seconds` 必须列在里面，
  漏了就会每次读档把计时清零 = 漏洞原地复活。新加实例字段时先想这件事。
* ⚠️ **不要在 `_claim_aquarium_bonus` 里写「没计时就放行」的老存档特例**：
  任何绕过「放入」直接写进缸里的鱼都会白拿加成（单测里就撞见过）。
  老存档迁移放在 `_load_player` -> `_migrate_tank_display()`，一次性补计时，
  之后运行期规则是干净的「够不够时间」。
* 视图里算进度要用 `_tank_display_seconds()`（含「这次还待在缸里」那段），
  直接读 `tank_seconds` 只有上一次结清的量，看起来永远不动。
* 页面/文案只说现象与门槛，别把内部字段名写进玩家可见文本。

**同款漏洞已在 v1.18.0 一起修掉**：`_pond_income(player, cfg, now)` 把「当前馆藏估值 ×
距上次领取小时数」换成「每条鱼按 `max(它进缸的时刻, 上次结算时刻) → 现在` 各自加权」，
空缸期间谁都不产出。`tank_seconds`（累计展出时长）随 ×1.2 加成一起删掉了，
现在只需要 `tank_since`。视图的「现在可领」也必须调**同一个** `_pond_income`，
口径分家就会再次出现「列表说能领、真领却没有」。

### 13. v1.18.0 踩到的几个坑（都很容易再犯）

1. **改默认值要同时改 schema**：运行时的配置是 `_conf_schema.json` 生成的，
   `DEFAULTS` 只是兜底。改了 `_game_data.BUTTON_DEFS_DEFAULT` / `item_defs`
   却忘了同步 schema 的 `default`，就会出现「代码里改了、跑起来没变」（这次就中了）。
   改完用一句话自查：`schema[key]["default"] == DEFAULTS[key]`（内容表按字节比）。
2. **短命令的参数形状**：`fishing()` 里 `after_sub = a2 + after_first`
   （`after_first` 是 rest[1:]，**不含 a2**）。短写法里 a2 就是第一个参数，
   所以路由时一律传 `after_sub`；传 `after_first` 会少一个参数，子命令就回退到「用法说明」
   （这次踩了 4 处）。子命令自己还要防一手：`/钓鱼 买 蚯蚓 3` 与
   `/钓鱼 商店 买 蚯蚓 3` 的 `a2/a3` 形状不一样，路由层负责对齐，别在子命令里猜。
3. **`_expose_globals_all()` 会把 `_calc` 的全局推回旧值**：main 在导入时把 `_calc` 的成员
   复制进自己的 globals，模块末尾又把它推回各兄弟模块 —— 所以任何「`_effects` 写给 `_calc`」
   的运行期状态都会被覆盖。`_calc._effect_allowed()` 现在**直接读注册表**绕开这个陷阱；
   同理，凡是要在运行期共享的可变状态（`BUTTONS` / `BUTTON_EMPTY_SCENES` /
   `BUTTON_COMMAND_EXTRA` / `SUBCOMMAND_WORDS`）都必须是**原地更新**的容器，不能重新赋值。
4. **删空按钮是一条链**：`BUTTONS`（配置里有什么）≠ `_scene_items()`（玩家实际看到什么）。
   后者还会「继承父场景 → 回退出厂」。要让「这个场景就是没按钮」生效，
   必须同时照顾 `_scene_items` / `_scene_buttons` / `_editor_default_buttons` 三处
   （`BUTTON_EMPTY_SCENES`），只改 `main._apply_button_defs` 是半成品。
5. **文案的「整段替换」开关**：`!!` 前缀是给站长的逃生口（放弃 `{原文}` 里的动态内容）。
   `needs_original()` 与 `render_scene()` 两处都要认这个前缀，页面校验也要跟着放行，
   否则会出现「页面说不行、其实插件认」的分裂。
6. **同类不相食**：`_resolve_tank_conflicts` 里比对手之前先比 `fish_id`，
   同种直接 `continue`（连「打了一架」也不该有）。改这段时记得单测里的
   「两条同种狠角色相安无事」与「两种狠角色才打」两条断言要一起过。

**v1.18.17**：站长一次提了七件事 ——
① 「把狠角色删掉，没意思、容易造成巨大损失」：整套机制（判定/配置/存档字段/文案/标注/
   编辑器行）连根拔除，见 §30；
② 「所有需要的地方都加上按钮」：官方按钮表 30 行 → 240+ 行 / 110 个场景，并新增
   「同组兜底」（没单独配的场景用同组基础场景那一组），老配置只补不覆盖，见 §29；
③ 鱼饵/手气道具用完**自动花钱补 + 自动装备**（金币不够绝不透支，手气道具要先指定用哪件）；
④ 批量命令不再受「形参只有 6 个」的限制（`/钓鱼 水族馆 放 1 2 … 12`），见 §31；
⑤ 订单价动态适配玩家收入（钓点 × 鱼竿 × 等级，封顶可配）；
⑥ `/钓鱼 钓点` 写出完整解锁条件（等级/金币/图鉴 + 你还差多少）+ 分页；
⑦ 后期金币回收：称号商店（6 档、纯炫耀）、香火供奉（20 万换 24 小时加成）、
   鱼缸再加 3 档终局缸（买满 39 格），挂机累计时长 12 → 24 小时。

### 30. 删掉一个「机制」要删哪些地方（v1.18.17，「狠角色」那次）

站长一句话「把狠角色这个设定删去，没意思，容易造成巨大损失」，但要删干净得动**七处**，
漏一处就会留下「幽灵功能」（配置还在、文案还在、玩家还能查到）：

1. **判定函数**（`_calc._is_hostile` / `_fish_power`）与它的输入（`HOSTILE_KEYWORDS`）；
2. **配置键**（`DEFAULTS` + `_conf_schema.json` + 编辑器 `NUMBER_KEYS` + `DEMO_NUMBER_VALUES`）
   —— 配置键数量在测试里是硬断言，漏改会直接红；
3. **存档字段**（`hostiles_seen`）：`_default_player` 删掉，`_repair_player` 里
   **读档时主动丢弃**老存档里的那个字段（不能只是不写，否则老数据会一直跟着存档走）；
4. **业务逻辑**（`_resolve_tank_conflicts` 的调用点与两个函数本体）与它产生的**文案**；
5. **游戏内查询**（`/钓鱼 查` 里的 ⚠️ 标注）与**用法说明**里那句「缸里的鱼之间会发生什么」；
6. **README / metadata / docs** 里的描述（含「隐藏的相处机制」这种卖点句）；
7. **测试**：把原来夸这个机制的断言**翻过来**（`hasattr` 必须为假、字段必须不存在、
   放两条鱼进去两条都在），而不是删掉了事 —— 删掉断言等于以后可以悄悄加回来。

### 29. 按钮的「同组兜底」与「只补不覆盖」（v1.18.17，站长要全场景按钮）

站长：「在不同的命令回复上添加按钮，应该是玩家有可能去使用的……所有需要的地方都加上」。
做法与规矩：

* **官方按钮表**给每个场景都写一组「下一步真会做的事」（空竿→换饵、体力不够→喝姜汤、
  背包满→卖/放/扩建、鱼缸→领/放全部/取全部/扩建）；
* **同组兜底**：`_calc.SCENE_BUTTON_BASE` 把「没单独配按钮的场景」映射到**同组基础场景**
  （`cast.miss_none → cast`、`aquarium.full → aquarium.view`、`item.feed_done → item.used`…）。
  解析优先级：**自己配的 > 显式父场景 > 自己的出厂按钮 > 同组基础场景**。
  ⚠️ 「自己的出厂按钮」必须排在「同组基础场景」前面，否则站长把基础场景改成别的样子时，
  子场景上那些精心配的按钮会被默默顶掉（这次就是这么定的）；
* **老配置只补不覆盖**：`button_defs` 进了 `CONTENT_TEXT_KEYS`，升级时按**场景 id** 去重、
  只补站长完全没有的场景（他配过的排版/样式/文案一个字不动）；
* **按钮白名单必须与路由同步**：`_calc.BUTTON_COMMAND_WORDS` 以前和 `SUBCOMMAND_KEYWORDS`
  各写一份，于是 `/钓鱼 称号` 这类新命令的按钮会被当「死按钮」丢掉（还不报错）。
  现在 main 在定义完子命令表后直接把它并进按钮白名单，测试也钉住两者的包含关系。

**v1.18.18**：站长问「这个游戏玩家一般从早玩到晚，道具你不做一些限制吗」——
之前只有「每条鱼每天 3 颗洗髓丹」这种单条鱼的限制，**全局没有天花板**：
手气道具可以整天挂着、姜汤可以无限喝（体力限制直接作废）。现在四类强度型道具各有
**每日额度**（`buff_daily_cast_limit` 120 竿 / `hot_soup_daily_limit` 5 次 /
`reroll_daily_total` 30 颗 / `offering_daily_limit` 1 次，全部 0 = 不限、跨天重置、
档案里能看到用量），**自动补给也守额度**，额度只剩一点时只给剩下的竿数（不吞整件道具）。
教训见 §32。

### 33. 给道具定价要拿 ROI 说话，别拍脑袋（v1.18.19，站长问「是不是太便宜了」）

站长一句「道具的价格是不是太便宜了，你自己考虑一下吧」。答案不能靠感觉，要**量出来**：
`ROI = 这件道具在中后期一次使用带来的收益 ÷ 价格`，目标 1.6~2.0（买它划算，但要付出代价）。

怎么量（`_task_item_roi.py` 那种一次性脚本，别留在仓库里）：

* **手气道具**：抽 4 万次品质掷骰，比较 `E[品质倍率]` 在有/无手气时的差 →
  `Δ倍率 × 每竿收益 × 覆盖竿数`。实测 +0.15 手气 = 价值 +25.6%、+0.20 = +33%、+0.35 = +58%。
* **饲料**：克隆一条该阶段的典型鱼，调 `_apply_feed` 前后取 `_instance_value` 差值。
* **洗髓丹**：三掷取最好（`_roll_quality_mult` + `_apply_quality`）后重算估值。
* **装饰**：用真实 `_pond_income`（**必须带 10 万单次封顶**，否则会把收益算成天文数字）。

这次量出来：潮汐香 8.4×、玉佩 4.1×、珊瑚王座 3.0×、洗髓丹 2.8×、玉髓灯 2.7×（都太便宜），
仙露 1.5×、龙涎 1.7×、造景 1.7×（本来就合理，不动）。
⚠️ 一口价道具 + 收益随鱼价上涨，必然导致「解锁时嫌贵、终局时嫌便宜」——
所以定价要**以终局收益为锚**（玩家到那时才买得起），并在文档里说清楚。

### 35. 「玩家说某个东西没用」时，先量分布，别急着调数值（v1.18.22）

站长一句「不用玉佩，玩家钓到的鱼也大部分是绝品了，你怎么看」。第一反应是「那就削玉佩」——
**错的**：量完发现玉佩在后期**一点用都没有**（+0.20 手气让绝品从 93.0% 变成 92.9%，纯噪声），
真正坏的是**手气 → 品质**的算法本身：

* 老算法 `point = rand*100 + 手气*90`，权重总和只有 100 —— 手气一过 **0.53**
  就把掷点整段推过前四档，概率全倒进绝品。于是中期（手气 0.55）就 52% 绝品、
  终局 93%，**手气的边际价值在高位是 0**（这同时是「后期金币溢出」的根：平均品质倍率 3.17）。
* 修法：手气改成**按档位放大权重**（第 i 档 ×`(1 + 手气 × luck_weight_step)^i`），
  手气 0 时逐字等于原权重；上限从硬编码 1.0 改成可配的 `luck_cap`；
  顺手把档位表顶部拉开（绝品 2.5-4.0 → 3.5-6.0），否则绝品变稀之后手气的价值会小到没意义。
* 教训一：**「什么东西太强」和「什么东西没用」经常是同一个 bug 的两面**，
  先跑分布（抽样 2 万次看各档占比 + E[品质倍率]）再决定动哪儿。
* 教训二：**改完必须回头核对定价**。手气道具的相对增益从 +33% 掉到 +4~10%，
  老价（12,000/18,000）立刻变成 ROI 0.3 的坑 —— 定价是「效果的函数」，
  效果一改就要用同一个口径（终局每竿 3,300 金 × ΔE/E × 竿数 ÷ 1.8）重算一遍。
* 教训三：**「绝品满地走」不只是手感问题，它直接决定了经济**：
  平均品质倍率就是收入倍率，3.17 → 2.38 = 后期收入 -25%。
  改爆率前先想清楚这 25% 要从哪里补回来。

### 34. 「同类效果不叠加」的边界要问清楚（v1.18.20）

站长说「同类效果不能叠加，比如说都加手气的道具」。真去改的时候发现**有三处都是同类相加**，
而其中一处（插曲一次性手气 + 玉佩）是他在 v1.18.11 **明确要求过相加**的 —— 直接按新话改
就是自己打自己脸。于是先问了一句，他回：「插曲，道具，供奉可叠加，但是手气道具两个不能叠加」。

规矩记下来：**「同类」的粒度是「同一类道具之间」**，不是「同一种数值来源」：

* 两件手气道具（玉佩/潮汐香/玉髓灯）→ **不叠加**，取较强的那个数值、时长取较长的那个；
  用更弱的一件只续时长；回复里写明这笔账（不然玩家以为被吞了）。
* 插曲一次性手气、香火供奉、道具手气 → **照旧相加**（`_effective_luck` 三项相加）。
* 鱼缸装饰（最多 3 个）→ 照旧相加（他早先确认过的设计）。

教训：改「叠加规则」这种跨系统的规则，先列清楚**所有同类来源**再动手，该问就问。

### 32. 给「强度型道具」加每日额度时的三个坑（v1.18.18）

1. **额度要在「使用」处扣，不是「生效」处扣**：手气道具是在 `/钓鱼 用 <玉佩>` 时
   一次性把 `buff_casts_left` 记满的，所以额度也必须在那时记 —— 否则会自动补给/连钓
   这种批量路径各算一遍，很容易漏记或者重复记。额度不够一整轮时**只给剩下的竿数**，
   别把整件道具吞掉（玩家会骂人）。
2. **自动补给必须读同一份额度**：`_auto_supply` 里如果只看库存与金币，限额就形同虚设
   —— 玩家挂机一整天，插件会一直替他买、一直续上手气。凡是「会自动花钱/自动消耗」的
   路径，都要先问一句「今天的额度还够吗」。
3. **惰性重置要顺手落盘**：`_daily_reset` 是按「日期字符串不等于今天就清零」实现的
   （和体力、洗髓丹一个套路）。查看类命令（档案/体力）也会触发重置 —— 那里如果只改
   内存不写盘，别的视图（编辑器玩家页）读到的还是昨天的计数。要么别在只读路径重置，
   要么像现在这样：**真的跨天了就顺手存一次**（一天最多多写一次盘）。

### 28. 「编辑器要能编辑所有东西」是怎么落地的（v1.18.16，站长那句「所有，所有」）
站长原话：「群钓鱼的插件编辑器要能编辑插件的所有东西，所有，并且还要把函数对应的功能
也写在里面方便我利用，里面的所有文字不要只能放在一行，如果字数太多就多行显示」，
另外「场景卡片里面，我加按钮应该是在已有的按钮里面选，而不是让我自己写」。

落地成了五条规矩，以后改编辑器照着来：

1. **配置键一个不漏**：页面 `NUMBER_KEYS` 必须覆盖 `_conf_schema.json` 的每一个键
   （v1.18.16 起 113 个），每条带「中文名 + 这个键到底管什么 + 入口」三样。
   入口只有三种：`""`（数值页直接改）/ `tab:xxx`（在别的标签页里可视化改）/ `panel`
   （插件配置面板项，页面只读）。新增任何配置键，**页面必须同时加一行**
   —— `test_editor_ui.js` 的 §[13b] 会拿 schema 逐键核对，漏一个直接红。
2. **白名单是最终权威**：页面能不能写由插件下发的 `numbers_editable` 决定，
   页面清单只是「入口」声明。`test_local.py` 的 `[16]` 两边都钉：页面说「数值页能改」的
   必须在白名单里，页面说「只读」的必须真的没被放行。
   ⚠️ 附带教训：`NUMBER_KEY_ALLOW` 里把 `backpack_upgrades` 拼成了 `backup_upgrades`，
   于是它被 `backup_` 前缀规则挡了好几个版本 —— **白名单这种「名字驱动」的规则，
   加键时要用测试把键名钉住**，别靠肉眼。
3. **文字必须能换行**：页面全页禁用 `text-overflow: ellipsis`，说明/提示/按钮/表格
   默认 `white-space: normal; overflow-wrap: anywhere`（只有数字列与 id 列保持单行）。
   长文案写在单元格里就该折行，而不是被切掉（§15 的窄窗口规矩仍然有效）。
4. **按钮从已有按钮里挑**：场景卡片的「＋ 加按钮」是**选择器**（文案/指令/样式三个下拉 +
   手动输入），下拉内容来自「已有按钮去重 + 插件下发的子命令 + 常用推荐」——
   底层写出来的仍是 `场景|文案|点击后发送|样式` 四段文本，格式一字未改。
5. **插件面板只读的键要写清原因**：现在只剩 7 个（`data_*` 是数据操作、两个 file 字段没有
   上传通道、`editor_status`/`data_status` 是页面自己的通道，读到的是过期状态）。
   这些键在页面上照样列出来并写明为什么只读，站长才不会以为是漏了。

顺带一条**游戏侧的排版规矩**（同一轮里做的）：帮助页按行数自动拆页
（`_views.HELP_PAGE_MAX_LINES = 16`，加上页头页脚正好 20 行），13 件道具那页自动变成
「道具 1/2」「道具 2/2」——「每一条回复的文本量都控制好」这条要求是**对游戏内所有回复**的，
不只对 `/钓鱼 查`。

### 27. 数值重设时，别只改 DEFAULTS（v1.18.16，道具重定价那次）

道具整套重定价 + 新增 6 件，涉及三件事，缺一样站长那边就看不到：

1. **改 DEFAULTS 里的内容表**（`item_defs`）；
2. **老配置里的旧行要靠迁移登记**：`content_auto_merge` 只补**缺的**行，
   已经存在的行永远不动 —— 所以「玉佩 500 → 6000」必须写进
   `LOCAL_CONTENT_ROW_FIXES`（或 `_game_data.CONTENT_ROW_FIXES`），否则站长手里
   永远是旧价（这次就靠它把玉佩/洗髓丹/育灵水升级过去的）。
   ⚠️ 只对**逐字等于旧默认**的行生效 —— 站长自己改过的行会保留，
   所以迁移完要**回头看一眼他的真实配置**，把「他没升级的那几行」在报告里点出来
   （这次是仙露/珊瑚造景/高级饲料三行，因为他自己改过）。
3. **一口价道具必须配等级门槛**：道具的收益随鱼价上涨，价格却不能分级 ——
   所以后期道具只能用 `unlock_level` 卡「什么时候买才划算」。
   这次给每件道具补了第 7 段（解锁等级），老配置没写第七段时按 1 级兜底。

### 26. 三个「看不见的坏掉」自查（v1.18.16，站长一次报了三个）

站长一次报了三条「功能没生效」，三条都是**不报错、不崩、只是悄悄不工作**的类型：

1. **扩容档位加了一档也没进来**：`aquarium_slots` / `backpack_upgrades` 明明登记在
   `DEFAULTS_MERGE_LIST_KEYS`（容器类合并）里，却又被写进了
   `DEFAULTS_SYNC_EXCLUDE_KEYS`（同步排除）—— 排除名单先被检查，合并逻辑
   **永远不会执行**。游戏侧拿不到、编辑器读配置也没有，表现就是「加了但看不见」。
   **规矩**：一个键只能属于一套升级机制。凡是进了 `DEFAULTS_MERGE_*` 的键，
   **绝不能**再进 `DEFAULTS_SYNC_EXCLUDE_KEYS`（`test_local.py` 的 `[10v]` 钉住了）。
   顺带把「文本形态的内容表」（杂物/变异/天气/彩蛋）也纳入了自动补齐
   （见 §21：`fish_defs` 当年就是这个坑，只是当时只修了它一张表）。
2. **自动存档看起来不工作了**：其实存档在跑，但 `data_status` 面板**自我嵌套**——
   `_refresh_data_status()` 把上一版整段面板塞进新面板的「上次操作」行，又把整段存回
   `self._data_status`，于是每 60 秒复制一遍，站长的配置里已经膨胀到几十 KB，
   WebUI 上那一段彻底没法看（他自然以为「坏了」）。
   **规矩**：① 面板每次**现拼**，绝不复用上一次的输出；
   ② `self._data_status` 只存「上一次操作」那一句短的；
   ③ 限长（`STATUS_MAX_CHARS`）；④ **内容没变就不写盘**（`test_local.py` 的 `[10w]`）。
3. **深水一半空竿**：不是概率坏了，是**数值太狠**：钓点系数从 1.0 一路滑到 0.54，
   配上最好的饵（1.0）也只有 54% 中鱼。现在改成 1.0 → 0.82（最深也有 82%）。
   **规矩**：给「难度曲线」定数值时，先算一遍**最好配置下的成功率**，
   别只看「越深越难」这个方向（`test_local.py` 的 `[6q]` 钉住了这一条）。

泛化的自查：**「我加了东西」和「玩家看得见」是两件事**。每次加内容/档位/功能，
都要问一句「它靠谁进到老配置里？进了之后靠谁显示出来？」，并且给这条链路写断言。

### 25. 游戏文本只服务游戏（v1.18.16，`/钓鱼 查` 扩查那次）

站长要求「`/钓鱼 查` 最好所有能查的都能查」，同时明确补了一句
**「游戏文本应该服务于游戏，对于开发者有用的就不用写出来在游戏里面」**。
于是这条命令的边界定成：

* **能查**：鱼、钓点、鱼饵、鱼竿、道具、杂物、变异、天气、成就 ——
  凡是玩家在游戏里能摸到、又需要一个「这玩意儿是干嘛的」解释的东西。
* **不能查**：插件配置键、回复场景键、内部函数/模块名、数据结构、存档字段。
  这些东西玩家问了也没用，写进游戏只会变成噪音（`test_local.py` 的 `[6p]` 里
  有一条断言专门扫「开发者向的词」有没有漏进回复）。
* 匹配规则：**精确同名优先**（查「锦鲤玉佩」不该被鱼「锦鲤」的模糊匹配抢走），
  命中多个就列表让玩家挑，命中一个才出详情卡；每张卡都限长
  （详情卡 ≤8 行、钓点卡 ≤14 行并指路 `/钓鱼 图鉴 <钓点>`）。
* 展示上**不给概率数字**：用星级/「更容易钓到」这类定性说法（老规矩，见 §13）。

### 24. 后期内容的加法：卖手感、别卖数值（v1.18.15，站长问「还要不要加竿加饵」）

站长问「数值平衡吗、后期要不要加新竿新饵」。**先算再答**（拿他的真实配置跑每竿期望收益），
结论摆在这儿，以后加后期内容照着来：

* 实测（他的配置：`sell_discount=0.7`、`fish_cost=0`、`pond_income_per_hour=0.03`）：
  新手空钩 1.8 金/竿 → 湖泊玉米粒 22.9 → 近海虾饵 72 → **海沟活饵 537（断层）** →
  极光秘制饵 1728 → 龙宫 3264。等级 62 级要 7293 条鱼 ≈ 1.3 万竿。
* **金币在终局严重过剩**：鱼竿全买满 4.1 万 ≈ 13 竿；扩容买满（背包 13.5 万 / 鱼缸 33.9 万）
  ≈ 41 / 104 竿。所以后期稀缺的是**时间与等级**，不是钱 —— 加内容的真正作用是
  「给一个花钱的地方 + 一段看得见的进度」，不是让老玩家更强。
* **别再用价值加成做升级**：每 +0.1 价值加成，终局收益就 +10%，会连锁推高钓点门槛。
  这次两档新竿的价值只 +0.03 / +0.06，主要卖**拉线手感**（窗口 +15%、逃脱率 -10%）——
  既让「拉线」这条玩法在后期有分量，又把收益膨胀压住了。这也是站长那句
  「越往后要越慢才能让后面的玩家追上来」在数值上的落法。
* **让新内容带取舍，别做纯上位替代**：两款专精饵把常见/少见权重压到 0.5/0.3，
  换传说/神话 7~10 / 10~14 倍。想刷图鉴或稳定收益就别用它 —— 有代价的选择才叫设计。
* **挂机收益的封顶要跟着收益曲线走**：5000 的封顶在 30 级以后等于零（龙宫一竿 3000+），
  整套养鱼玩法因此报废。改成 10 万/次（≈ 终局 30 竿）后它是补充而不是捷径。
  教训：**凡是「固定上限」，都要问一句「后期这个数还够看吗」**。
* 加竿/加饵的**落地清单**（不写全就会「加了但没生效」）：
  `DEFAULTS["rod_defs"]/["bait_defs"]` + `_conf_schema.json` 默认值 +
  `bait_hook_rates` 补键 + `RODS` 兜底表（数值必须与 rod_defs 一致，[6n] 会断言）+
  编辑器页面的离线演示数据 + README 表格 + `test_local.py` 的数量断言（鱼竿 6→8、鱼饵 8→10）。
  内容表本身靠 `content_auto_merge` 自动补到老配置，不用站长重置。

### 23. 同一个东西存在两份 / 两条路径（v1.18.14，手气那次又栽了一次）

站长只是问「手气的具体效果」，顺着读代码就抓出两处同类问题：

* **两条路径口径不一致**：不拉线的鱼 `手气 = 鱼饵+鱼竿+天气+一次性+玉佩`，
  而拉线成功后掷品质只传了「鱼饵+天气+评价加成」—— 鱼竿与玩家手气被吞，
  可收尾照样 `_consume_luck()`（**花了钱没效果**，比「少给」更糟）。
  修法是让两条路径共用同一份 `spec`/公式，并用 spy 断言两边实参一致。
* **同一份数据存了两份**：兜底鱼竿 `RODS` 与 `rod_defs` 各写一套数值，
  一旦配置被清空/写坏，鱼竿就悄悄换一套（价格、手气全变）。
  修法是让兜底**逐项等于**默认配置，并加断言钉住。
* 自查清单（每次加机制时过一遍）：
  **① 这件事在别的路径上是怎么算的？② 这份数据在别处有没有第二份？**
  只要答案是「另一套」，就迟早会出现这次这种「一边生效一边不生效」。
  两条都已经在 `test_local.py` 里钉住（[6n]）。

### 22. 给「内容表 + 存档状态」加新维度时的自查清单（v1.18.13，连载那次的规矩）

站长要「事件连成连续剧」时，真正的工作量不在写文案，而在**把新状态安全地接进存档**。
下次加「需要记住历史」的玩法（连载、成就线、每日任务链…）照着这份清单过一遍：

* **存档字段要进白名单**：`_calc._repair_player` 是**白名单式重建** ——
  新增 `story` 之后必须在那里补一段解析，否则「今天演到第几话」每次读档都被丢掉
  （`tank_since` / `reroll_day` 都栽在这上面）。
* **状态要能自愈**：内容表里删掉一条线/一个事件之后，存档里可能指着不存在的东西 ——
  读档时清掉（`arc` 不在 `CHAIN_BY_ID` 就归零、`event.id` 不在 `EVENT_BY_ID` 就丢弃），
  否则玩家会卡在一条永远演不下去的线上，而且没有任何报错。
* **「随机了什么」也要存**：选项位置打乱之后，**顺序本身就是状态** ——
  提示、按钮、`/钓鱼 事件 N` 三处必须读同一份 `order`，
  否则玩家点「1」选中的是另一个选项（这种 bug 极难复现）。坏数据一律退回自然顺序。
* **可配置 + 可关**：新玩法至少给一个概率/开关（`story_chain_chance` / `story_chain_gap` /
  `story_shuffle_choices`），并且进 `_conf_schema.json` + `DEFAULTS` + 编辑器 `NUMBER_KEYS`
  —— 三处齐了 `test_local.py` 第 [16] 组才不会红。
* **改「内置写法」等于改配置契约**：这次给子命令表加了三家店和一个「买」，
  直接引出一个真问题 —— 老配置里的 `command_aliases` 写着 `商店|鱼饵,道具,shop,买`，
  如果 `买` 不是内置写法，它就会被别名劫持，玩家 `/钓鱼 买 蚯蚓` 直接买不到东西。
  凡是「新增/改名内置子命令」，都要回头看一遍老配置的别名行能不能劫持它，
  并让 `_build_command_aliases` 挡住（内置写法抢不走）。
* **老命令的退场方式**：`/钓鱼 商店` 不是删掉（玩家会一脸问号），而是留一个**指路回复**，
  并且尽量带上他原本想干的事（`👉 买「蚯蚓」现在发：/钓鱼 鱼饵 买 蚯蚓`）。
  场景也照旧登记（`shop.moved`），这样站长能单独配文案。
* **内容表加字段要么兼容、要么解析容错**：鱼缸扩建新增第三段 `名字|价格|加几位`，
  `_parse_aquarium_slots` 必须让「两段的老写法」照常工作（默认 +1），
  老配置才不会突然少格子。

### 21. 「内容升级」要把所有形态的内容表都覆盖到（v1.18.12，新钓点空池那次的教训）

站长报：新加的星陨湖/万水归墟/龙宫「进得去，但里面一条鱼都没有，图鉴也是空的」。

**病根**：升级时补内容的那段只遍历 `CONTENT_LIST_KEYS = (location_defs, rod_defs,
bait_defs, item_defs)` —— **`fish_defs` 不在里面**。两条原因叠在一起：

1. 它不是 list，而是**一整段多行文本**（`id|名称|稀有度|…` 换行分隔），
   通用合并的第一步 `if not isinstance(current, list): continue` 就把它跳过了；
2. 它**非空就整体接管鱼池**（`_apply_fish_defs`），所以「配置里那份是旧快照」
   ＝官方新增的鱼一条都没有，而不是「用内置的补齐」。

于是钓点靠 list 合并补上了、鱼却卡在文本里 → 空池。**这类 bug 不会报错、不会崩**，
只会静默地少内容，所以一定要有体检（见下）。

**规矩**：

* 加/改任何「内容型」配置时，先问三句：
  **① 它是 list 还是文本？② 它怎么被消费（合并 / 整体接管 / 只读）？
  ③ 升级时它靠谁补？** 三个答案只要有一个是「没想过」，就会出现这次这种空池。
* 自动补内容时的**边界**（这次特意划清的）：只补「看起来就是官方旧快照」的，
  判据 = 配置里**至少一半的行是官方 id**；站长从头手写的自定义内容一个字都不动。
  宁可少补，也不能把站长的自定义淹没掉（`test_local.py` 的 `[11b]`/`[10u]` 都钉着）。
* **改过的一行**推不到老配置（只补缺的行），要么在 `_game_data.CONTENT_ROW_FIXES`
  登记「旧整行 → 新整行」，要么接受老值。这次顺手修了这条链路本身：
  `_apply_content_row_fixes` 以前也只认 list，字符串形态（`fish_defs`）**直接跳过**，
  等于「官方改过的鱼行永远推不到老配置」。
* **体检要能喊出来**：`_warn_stale_config` 以前只查「钓点少了几个」，
  查不出「鱼池是旧快照」。现在会写「少 39 种鱼 → 这些钓点会一条鱼都钓不到：…」。
  凡是「静默少内容」的升级路径，都该有一条对应的体检日志。
* 复现站长环境的办法（也是 `[10u]` 的做法）：**拿当前默认池减掉新加的内容**，
  造一份旧快照塞进 `fish_defs`，断言补完能回到 `len(BUILTIN_FISH_POOL)` 且新钓点非空。
  「默认配置下正常」≠「老配置升级后正常」，测试必须造出**老配置**。

### 20. 同一个机制只留一条路径（v1.18.10，连钓与单竿分家的教训）

站长报：「连钓的鱼居然不会跑……连钓最多有鱼没咬饵，这个 bug 很严重」。
两条现象、一个病根：**单竿与连钓各写了一套判定与文案，谁也不管谁**。

| | 单竿 `_do_cast` | 连钓 `_do_cast_multi`（修之前） |
| --- | --- | --- |
| 逃脱 | 弹拉线，手慢/超时 = 必跑（传说 30% 是「没拉到」的概率） | 直接 `random() < spec["escape"]` → 同一批鱼**几乎不跑** |
| 空竿文案 | 空钩 / 深水 / 「咬了一口又吐掉了——蚯蚓 白搭了」 | 只有一句「💨 空竿」 |
| 空竿扣饵 | 被咬掉的照扣 | 一个都不扣 |

结果就是「认真拉线的单竿会跑、连钓不跑」，连钓成了刷传说鱼的捷径；而玩家看到
「连钓从不吃饵」也确实是代码写的。**修法不是各修一处，而是把口径抽成共享函数**：

* `_calc._miss_flavor(bait_id, label, loc, factor)` → `(场景键, 文案, 是否被咬掉)`，
  三种说法（空钩 / `钓点系数 < 0.75` 的深水 / 被鱼咬掉）**只在这里写一次**，
  两条路径都拿它的返回值去 `_say`；
* `_calc._bait_consumed(...)` → 扣饵真值表：`每竿都扣(开关) or 中鱼/杂物 or 被咬掉`；
* `_calc._multi_escape_chance(spec, cfg)` → 连钓专用惩罚：`标称逃脱率 × multi_escape_mult`
  （默认 2.5，封顶 0.95 —— 连钓里也不该存在「必跑」）。**默认值必须让手感变狠**
  （1.0 = 旧行为），否则老配置升级后等于没修；
* 跑掉的那条要**把实际用的概率写出来**（`逃脱率 75%`），不然玩家没法判断是运气还是配置。

自查一句：**「这个机制在另一条路径（单竿 / 连钓 / 批量）上是怎么走的？」**
只要答案不是「同一个函数」，就迟早会出现同一批鱼两套概率 —— 而且会先被站长发现。

⚠️ 静态检查的连带坑：场景键（`cast.miss_*`）现在只在 `_calc.py` 里出现字面量，
`test_local.py` 第 [18] 组扫「死场景」的 `_src_all` 必须把 `_calc.py` 也带上，
否则三个明明在用的场景会被判成死代码。

⚠️ 写用例的连带坑：像 `LOCATION_HOOK_FACTORS` 这种**模块级字典**是「最后一个造出来的
插件说了算」（`_refresh_config` 原地 `clear()` + `update()`），不是挂在 `self.cfg` 上。
所以同一段用例里造第二个插件，会顺手改掉第一个插件后续的判定 ——
`[6l]` 里「咬掉（0.9）」必须在造出「深水（0.5）」那个插件**之前**跑完，
否则连钓会被判成「鱼不开口」、饵一个不扣（这次就是这么红的）。
凡是 `self.cfg.get(...)` 之外的口径，用之前先确认它存在哪。

### 19. 「按次数算」的东西，连钓路径要按竿数循环（v1.18.9）

站长报：连钓 15 次，锦鲤玉佩只掉 1 竿额度 —— 等于白吃 14 竿手气。

原因：`_engine._do_cast_multi` 在循环**外面**调了一次 `_consume_luck(player)`，
把「整批」当成「一竿」；而体力、鱼饵都是按 `planned` 扣的 —— 同一个方法里两套口径。

**现在的规矩**：凡是「消耗 / 剩余次数」类的东西，在连钓里都必须**循环内结算**：

* `luck = _effective_luck(player)` + `_consume_luck(player)` 放在每次 `for index in range(1, planned+1)`
  的最前面（空竿、杂物也算一竿，和体力/鱼饵一致）；
* 一次性手气（插曲/彩蛋）因此天然只作用于第 1 竿 —— 这正是想要的语义；
* 结果里要写清消耗了多少：`🎐 锦鲤玉佩：本批 -N 竿　还剩 M 竿`（用完写「（用完了）」），
  没有 buff 时不显示这一行（不占版面）；
* 加新机制时自查一句：**「这个东西在连钓里该怎么算？」**
  —— 按竿数算的（体力/饵/手气/掉落计数）放循环里；按整批算的（扣费上限、背包余量）放外面。

### 18. 升级默认值不能覆盖站长改过的东西（v1.18.8）

站长反馈：「更新插件不要把我以前的配置重置，我老是要去改」。
`defaults_sync_mode=auto` 以前是「指纹变了就把所有同步项写回新默认」——他手改的数值全被冲掉。

**现在的规矩**（`main._sync_defaults`，改同步逻辑前必读）：

1. `user_edited_keys`（编辑器每次保存都会记一笔）里的键**一律跳过**；
2. 容器类配置**合并**：`DEFAULTS_MERGE_MAP_KEYS`（`键:值` 映射，如
   `location_hook_factors` / `bait_hook_rates` / `rarity_*`）按**键**补缺，
   `DEFAULTS_MERGE_LIST_KEYS`（真列表，如 `quality_weights`）与
   `DEFAULTS_MERGE_NUMBER_TEXT_KEYS`（`tier_base_values` 这种 `1,2,3` 文本）按**长度**补尾巴。
   → **新增钓点/新增品质档位必须走这三类之一，否则到不了老配置**；
3. 官方改了默认值的「整串」配置登记到 `DEFAULTS_VALUE_FIXES`（键 -> ((旧默认, 新默认), ...)），
   只有当前值**逐字等于旧默认**才替换 —— 这次品质档位表改名就靠它；
4. 还有一条兜底：如果 `user_edited_keys` 是空的（第一次启用这套机制），
   就认为「和新默认不同的值」都是站长改过的，**全部保留**并记进清单。
   代价：这次官方改过、又没登记进 VALUE_FIXES / 合并键的默认值不会自动生效 ——
   **所以改了默认值一定要顺手登记**（`test_local.py` 的 `[10t]` 段把这几条都钉住了）。
5. `defaults_sync_mode`：`auto`（默认，上面这套）/ `all`（强制重置，站长明确要的时候用）/
   `off`（什么都不动，只更新指纹）。

### 17. 「只能靠某件道具拿到」的东西怎么实现（v1.18.7，神话那次的规矩）

站长要一档「神话」个体品质，**只能洗髓丹洗出来**、自然上钩永远没有。做法与坑：

* **自然抽奖那头把权重写 0 还不够**：`_roll_quality_mult` 原来的默认档是
  `len(weights) - 1`，而「运气值」会把随机点推到 `total` 之外 —— 点溢出时正好落在
  默认档上。所以权重 0 的档必须同时满足两条：`weights[i] == 0` 时**跳过累加**，
  且默认档 = **最后一个权重 > 0 的档**。少改一条，运气爆棚时就会白送神话。
* **钳制值要跟着可配置的最高档走**：`quality_mult` 原先在 `_new_instance` /
  `_apply_quality` / `_repair_instance` 三处写死 `5.0`，加一档 6.0 的话
  洗出来的 6.0 会在下一次读档被削回 5.0。现在统一走 `_calc._quality_ceil()`
  （= `max(5.0, 最高档上限)`）。
* **「唯一产出路径」只留一处**：神话只由洗髓丹分支里的 `quality_myth_chance` 判定，
  命中后**直接取最高档的区间**（不硬编码 6.0），这样站长改 `quality_tiers` 也仍然成立。
* **次数限制挂在「被限制的对象」身上，且必须进白名单**：`reroll_day` / `reroll_today`
  记在**鱼实例**上（跨天靠日期比对自动作废，不需要定时任务）。
  `_repair_instance` 是白名单式重建 —— 漏写这两个字段 = 每次读档把当天计数清零，
  「每天 3 颗」直接失效（`tank_since` 当年就是同一个坑）。
* **被拒绝时不能扣道具**：先判 `_reroll_averse`，厌恶就 `continue`
  （收集起来在末尾说明），扣道具放在真正洗完之后。
* 老配置里的那一行道具（洗髓丹）改了价格/说明，必须登记到
  `_game_data.CONTENT_ROW_FIXES`（内容表不参与默认值同步），否则站长那边永远是旧价。
* 新增的数值键（`reroll_daily_limit` / `quality_myth_chance`）除了 DEFAULTS +
  `_conf_schema.json`，还要进编辑器页面的 `NUMBER_KEYS` ——
  `test_local.py` 第 [16] 组会断言「面板藏起来的键在编辑器里都能改」，漏了直接红。

### 16. 一个字段别装两种寿命（v1.18.5，手气那次的教训）

**症状**：随机插曲给的「下一竿手气」不消失，还和锦鲤玉佩叠着算（站长报的）。

**病因**：`luck_charges` 一个字段同时装了两样东西：

* 玉佩 = **持续 20 竿**（每竿都该加）
* 插曲/彩蛋 = **一次性**（下一竿用完就没）

而抛竿收尾那段是「玉佩还在生效（`buff_casts_left > 1`）就不清储备」——
于是插曲那份被当成玉佩的额度，挂满一整轮，还每竿都加在玉佩上面。

**现在的规矩**（改手气相关代码前必读）：

* `luck_charges` 只装**一次性**的：每次抛竿收尾一律清零（`_calc._consume_luck`），
  不管玉佩在不在；插曲 / 彩蛋 / 扩展都往这里写。
* `buff_quality`（新）+ `buff_casts_left` 装**持续型**：抛竿取值走
  `_calc._effective_luck(player)` = `一次性 + 玉佩`（**叠加**，v1.18.11 起）；
  竿数减到 0 时把 `buff_quality` 一起清掉（别留「还剩 0 竿但加成还在」的空 buff）。
  ⚠️ v1.18.5 曾把这里写成 `max(一次性, 玉佩)`（不叠加）—— 那是修「赖着不走」时
  顺手多禁了一层，站长要的是「可以叠，只是一竿后事件那份就没了」，v1.18.11 改回来了。
* 展示用 `_views._buff_status_line()`：两者同时在时**把这一竿的合计算给玩家看**
  （「一次性 +50%（叠加：下一竿共 +80%，之后回到 +30%）」），
  别只列两个数字让玩家自己猜是加还是取大。
* 写扩展/新道具时照这个分工来：只写 `buff_casts_left` 不写 `buff_quality`
  = 有竿数没加成（`extensions/example_effect.py` 就是模板）。
* 泛化的教训：**一个字段只装一种寿命**。「持续 N 次」和「下一次」必须分开存，
  否则任何「按剩余次数决定要不要清」的判断都会把短寿命的那份续成长寿命。
* **修 bug 时别顺手改掉玩家能感知的其它行为**（v1.18.11 补的教训）：
  上面这次为了「一次性别赖着不走」，把取值从「相加」改成了 `max()` ——
  寿命问题确实修好了，但也**顺手取消了叠加**，而站长要的恰恰是叠加。
  正确的最小修法是「只动寿命，不动叠加」。判断标准很简单：
  **这次改动里，有哪一条是玩家能看见、而我没被要求改的？** 有就说明改多了。
  另外：`max()` / 取较高这类「防叠加」写法，本质上是在绕开脏数据 ——
  字段拆干净之后就该拆掉它，否则它会伪装成「设计如此」一直留着。

### 15. 编辑器页面的窄窗口规矩（v1.18.3 / v1.18.4，改页面 CSS 前先看这条）

AstrBot 的插件页是塞在 dashboard 的 iframe 里的，实际可用宽度经常只有
700~1100px（不是站长显示器的宽度）。所以：

* **不要写死表格的 `min-width` + 一堆固定列宽**：宽度一旦超过容器，右边几列就被切到
  横向滚动条外面，站长看到的就是「有些列怎么也看不到」（他报过一次）。
  现在的做法：所有表格都带 `stack` 类、外面套 `.table-wrap.is-stack`，
  `@media (max-width: 1180px)` 里改成「一行一张卡」（`thead` 隐藏、
  `td::before { content: attr(data-label) }`），所以**每个 `<td>` 都必须写 `data-label`**
  —— 漏了的话窄屏那格就没有标签，看不懂是什么。新增表格时照抄
  `renderTableRow` 的写法。
* **不要用视口高度把内容框死**：`max-height: calc(100vh - N)` 在矮 iframe 里会把表格
  压成一条缝。要带下限：`max(360px, calc(100vh - 300px))`（`.table-wrap` 已经这么写）。
* **窄屏断点**：≤1400px 回复页的预览面板挪到卡片下面（右侧固定 330px + 主列会挤坏表格）；
  ≤1180px 表格拆卡片；≤820px 顶栏不吸顶（换行后高度估不准，会压住内容）、
  底部状态栏允许换行（否则右边信息出屏）。
* `.rp-side` 是 sticky 的：必须配 `max-height: calc(100vh - 118px); overflow: auto`，
  否则面板比窗口高时，下半截顶在屏幕外**永远够不着**。
* **长列表别在一行里显示**（v1.18.4，站长报的）：数值页那些「逗号分隔的名单」
  （`location_hook_factors` 16 项、`bait_hook_rates` 8 项、`tier_base_values`…
  都是 `NUMBER_KEYS` 里第 5 位为 `true` 的**文本行**）如果渲染成一整行字符串，
  那一行的 min-content 宽度会把整张表撑开、别的列被挤走。现在的做法：
  `cellHtml` 里 `row.text` 且条目 >3 个（或字符串 >24 字）就渲染成 `.kv-chips`
  小方块（`max-width: 360px` 兜住宽度），编辑时换成 `<textarea class="is-multiline">`
  并**一行一条**（`_split_list_text` 把换行也当分隔符，所以往返安全）。
  配套：`keydown` 里 textarea 的 Enter 必须是换行，提交走 Ctrl/⌘+Enter。
* 测试口径：`test_editor_ui.js` 的「窄窗口布局」段与「长列表」段把上面这些规则和
  `data-label` / `.kv-chips` / `textarea.is-multiline` 都钉住了，
  改页面 CSS 或表格渲染后跑 `node test_editor_ui.js` 就知道有没有破。
* **整页重绘要保住滚动位置**（v1.18.6，站长报的「点一个项再点另一个就跳回顶部」）：
  `render()` 会先 `snapshotScroll()`、渲染完 `restoreScroll()`，范围包括窗口滚动条、
  `.table-wrap` / `.rp-table-wrap` / `.rp-side`。新增「只换一小块 DOM」的局部更新路径时，
  也要自己把 `scrollTop` 存一下（搜索框那条路径就是），否则一样会跳。
  只有**明确要回顶部**的地方（切标签页）才写 `render(false)`。
* **数值页要「算给你看」**：站长提过「升级二次曲线没有推荐值，不知道怎么填」。
  凡是光看名字猜不出该填多少的数值，就在 `numberLiveHint()` 里加一条——
  按当前输入值实时算出结果（和插件侧公式对齐）、给出推荐值与几个档位的效果对比。
  升级曲线那三行已经这么做了，加新提示时记得同步 `_conf_schema.json` 的说明。

### 14. 存储：AstrBot KV 本来就是 SQLite，别再引第二套库（v1.18.1）

站长提过「改用 sqlite 存数据」。查过真实运行环境后确认**已经在用 SQLite**，
所以没有换存储 —— 自己再开一个库只会多一份要备份/要迁移/会读脏的数据，
而且会绕过 AstrBot 的 KV 接口（`get_kv_data` / `put_kv_data`）。

* 数据位置：`<AstrBot 根>/data/data_v4.db`，表 `preferences`
  （`scope='plugin'` / `scope_id=<插件id>` / `key=<键>`），插件侧的键是
  `player_<uid>` / `player_index` / `leaderboard`。
* **值的形状要照实抄，别想当然**（这次差点写错，靠只读查真库才发现）：
  * 行是 `{"val": <值>}`，而**旧作用域里的 `<值>` 是一个 JSON 字符串**
    （不是嵌套对象）：`{"val": "{\"user_id\":\"10001\",\"gold\":90,...}"}`。
    ⚠️ 写文档时**别把真库里的 uid / 昵称抄进来**（这里是编的 id）：那是玩家的 QQ 号，
    仓库要公开的话就成了泄露。
    所以 `unwrap_kv()` 之后**还要再 `json.loads` 一次** ——
    只按「dict 里取 data」写，扫描出来全员金币 0（页面看着像数据坏了）。
  * 当前作用域的那个字符串里是**信封**（`__fishing_player__` + `data`）；
    旧作用域里是**裸玩家 dict**（`data_version: 3`，没有信封）。
    两种都要认：读摘要用 `parse_player_value()`，搬运用 `normalize_player_value()`
    （裸 dict 会先包成信封，否则页面/存档那条只认 dict 的链路看不见这名玩家）。
  * `player_index` 的 `<值>` 也是 JSON 字符串（`"[\"id1\",\"id2\"]"`），
    `_legacy.as_id_list()` 两种写法都认，写回时保持原格式。
  * 真库里还有 `player_<@A1B2C3D4...>` 这种**脏键**（uid 被打成了「@某人」）：
    `normalize_uid()` 取里面的干净 id 再落地，否则搬进来就是一条永远读不到的废数据。
    （例子里的 id 是编的 —— 文档里不要出现真 uid / 昵称。）
* **插件 id 变了 = 老数据变成孤儿**：作者名从 `dhhxfggg` 改成 `dhhxfggg2023` 时，
  `plugin_id` 跟着变，KV 按 `scope_id` 隔离 → 老存档读不到，表现就是
  「那几名玩家的进度突然从头开始」。这不是数据丢了，还在旧 scope 里躺着。
* 找回工具：`_legacy.py`（`legacy_scan` 只看 / `legacy_import` 只补缺的），
  页面入口在「👤 玩家」页右上角「🔍 找回旧数据」。
  * **只读打开主库**：`sqlite3.connect("file:...?mode=ro", uri=True)`，
    老作用域一行都不动、不删（站长明确要求「旧的先留一份」）。
  * 只补 `player_*`，当前作用域已有的**一律跳过**（当前数据优先）；
    导入前自动存一份 `manual` 快照。
  * 读不到库时把原因记进 `_legacy.LAST_ERROR`，页面照实显示
    （「数据库文件不存在：…」），**不许装成「没有旧数据」**。
  * 面板必须**关得掉**（站长反馈过一次「关不掉」）：扫描结果缓存在
    `self._editor_legacy_cache` 里并随 `editor_status.legacy` 每次下发，
    所以只清页面状态没用 —— 得配一个 `legacy_clear` 动作把插件侧的缓存也清掉，
    否则刷新一次页面又冒出来。这个动作**只清缓存，不碰数据**。
  * `player_index` 要**合并**而不是覆盖（见上面的字符串形状）。
* 新增/修改 `player_*` 之外的老键（`leaderboard` 等）目前**不导入**：
  排行榜是缓存，重算就行；真要导入得先想清楚合并规则。
* 测试口径：`test_local.py` 的 `[10q2]` 段用沙箱里的假库（含信封 + `{"val":...}`）
  覆盖「只读同名插件的旧 scope / 标出缺谁 / 只补缺的 / 索引合并 / 老库零改动 /
  重复导入不写第二遍」——**绝不允许**在测试里连真实 `data_v4.db`（哪怕是读）。

