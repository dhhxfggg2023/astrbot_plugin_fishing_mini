feat: 编辑器在线通道 + 存档管理后端

- 新增 _editor_bridge.py：页面 → 插件指令通道（固定文件名上传 + 1.5s 轮询 + nonce 去重 +
  启动时播种 nonce 防重放）+ 插件 → 页面状态回写（editor_status）；8 个动作白名单；
  initialize/terminate 安全启停，坏 JSON 与未知 action 也消费并回写错误
- 更正方案里的错误假设：SDK upload() 落点是 <ASTRBOT_ROOT>/data/attachments/（不是插件目录），
  已按 AstrBot 源码核实修正
- _backup.py 新增 rename_snapshot（只改 note 不动文件名，保持「最新 = 按 mtime」语义）
  与 delete_snapshot（幂等）
- i18n 真实路径是 .astrbot-plugin/i18n/，页面标题「数据编辑器」
- editor_ 加入 DEFAULTS_SYNC_EXCLUDE_PREFIXES，否则 editor_status 会被默认值指纹同步重置为空
- pages/editor/index.html 的 4 个预留函数接入真实通道（保留离线预览模式）
- README 新增「数据编辑器页面」一节（入口、保存通道原理、存档管理、离线预览）
- 新增断言：test_local[10q]、test_editor_ui[12][13]、loader[7]、
  docker_deploy[1b][1c]（entrypoint 不会误删 pages/ 与 .astrbot-plugin/）、CI 冒烟 +2 条

验证：五套 Python 测试全绿（700/70/50/36/26 断言）+ node test_editor_ui.js（141 项），
py_compile 全模块通过；配置项 86 项，schema 与 DEFAULTS 一致

已知限制
- 在线页面尚未在真实 WebUI 中人工点验（离线渲染的视觉验收已完成，见 docs/screenshots/）
- 存档页暂缺「只恢复单个玩家」输入框（通道与后端已支持 player 字段）
- 存档「下载」按钮下载的是存档信息，玩家数据本体仍走「插件配置 → 导出存档文件」
